type Item {
  id: ID!
  sku: String!
  price: Float!
}

type Cart {
  id: ID!
  items: [Item!]!
  user: User!
}

type User {
  id: ID!
  name: String!
}

type Query {
  items: [Item!]!
  cart: Cart!
}

input NewItem {
  sku: String!
  price: Float!
}

type Mutation {
  createItem(item: NewItem!): Item!
  addItemToCart(sku: ID!): Cart!
  checkout(cart_id: ID!): String!
}
